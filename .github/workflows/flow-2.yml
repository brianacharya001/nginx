name: Flow 2 - After Flow 1

on:
  workflow_run:
    workflows: ["Flow 1 - Event Driven"]
    types:
      - completed

jobs:
  job2:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted
    steps:
      - name: Run Flow 2
        run: echo "Flow 2 triggered after Flow 1 success"


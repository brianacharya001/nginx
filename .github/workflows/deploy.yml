name: Deploy Nginx (Fail & Delete Failed Revision)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set kubectl context
      run: kubectl config use-context minikube

    - name: Set deployment revision
      run: |
        REVISION=$(echo "${GITHUB_SHA}" | cut -c1-7)
        echo "REVISION=$REVISION" >> $GITHUB_ENV

    - name: Inject revision into manifests
      run: |
        sed -i "s/{{REVISION}}/${REVISION}/g" manifests/bad-deployment.yaml

    - name: Apply manifests (may fail)
      run: kubectl apply -f manifests/

    - name: Monitor rollout and delete failed revision
      run: |
        set +e
        kubectl rollout status deployment/nginx-deployment --timeout=60s
        STATUS=$?
        set -e

        if [ $STATUS -ne 0 ]; then
          echo "❌ Deployment failed"
          echo "🗑️ Deleting failed deployment revision to clear pods"

          kubectl delete deployment nginx-deployment
          
          echo "✅ Failed deployment deleted — previous pods remain unaffected"
          exit 1
        fi

        echo "✅ Deployment successfully completed"

    - name: Show final deployment state
      run: kubectl get deployments,pods

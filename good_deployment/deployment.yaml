apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app-deployment
  labels:
    app: flask-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
        app.kubernetes.io/revision: "{{REVISION}}"
    spec:
      containers:
      - name: flask-container
        image: python:3.9-slim
        command: ["/bin/sh", "-c"]
        # In 2026, we must install dependencies if not using a custom pre-built image
        args: 
          - |
            pip install flask
            python -c "
            from flask import Flask
            import os
            app = Flask(__name__)
            @app.route('/')
            def hello():
                rev = os.getenv('APP_REVISION', 'unknown')
                return f'Revision: {rev}'
            app.run(host='0.0.0.0', port=5000)
            "
        ports:
        - containerPort: 5000
        env:
        - name: APP_REVISION
          value: "{{REVISION}}"
        # Liveness probe helps ensure the container is truly healthy
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20
